import { ProfileThemePipe } from './profile-theme.pipe';

describe('ProfileThemePipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileThemePipe();
    expect(pipe).toBeTruthy();
  });
});
